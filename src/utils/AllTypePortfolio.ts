export interface SocialIconData {
  name: string;
  path: string;
  icon: JSX.Element;
}
export interface ProjectItem {
  category: string;
  id: number;
  img: string;
  title: string;
  description: string;
}
